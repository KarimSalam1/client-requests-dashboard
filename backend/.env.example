PORT=4000
# A hosted MongoDB Atlas cluster is pre-configured, so this runs with no local DB setup.
# Copy this file to .env (`cp .env.example .env`) and start the app.
MONGODB_URI=mongodb+srv://test:1234@karimcluster.wx87vwl.mongodb.net/client-requests?appName=KarimCluster
CLIENT_ORIGIN=http://localhost:5173
